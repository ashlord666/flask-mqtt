version: '3'
services:
  flask_app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - MQTT_BROKER=mosquitto
      - MQTT_PORT=1883
    depends_on:
      - mosquitto
  mosquitto:
    image: eclipse-mosquitto
    ports:
      - "1883:1883"
